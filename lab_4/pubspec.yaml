name: lab4
description: "Lab4"
publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  table_calendar: ^3.0.9
  flutter_map: ^6.1.0  # For OpenStreetMap functionality
  latlong2: ^0.9.0     # For handling map coordinates
  geolocator: ^10.1.0  # For geolocation services
  flutter_local_notifications: ^16.2.0  # For reminders and notifications
  intl: ^0.18.1        # For date formatting
  provider: ^6.1.1     # For state management
  http: ^1.1.0         # For any potential network requests
  url_launcher: ^6.1.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true